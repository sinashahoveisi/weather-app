import type {FC} from 'react';

interface Props {
  headerText: string;
  subHeaderText: string;
}

const WeatherCityCard: FC<Props> = ({headerText, subHeaderText}) => {
  return <p>sasa</p>;
};

export default WeatherCityCard;
